{
  "version": "1.8.4",
  "versionCode": 17,
  "url": "https://maikerufurora.github.io/pesoflow/peso-flow.apk",
  "notes": "In-app update flow — tapping the update banner now downloads the APK inside the app with a live progress bar, then fires the system installer directly. No more browser hop, no more pesoflow(2).apk clutter. APK size dropped from ~25 MB to ~9 MB thanks to split-per-ABI + obfuscation (arm64 build for modern Android)."
}
